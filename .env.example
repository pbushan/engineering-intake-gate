# Optional, non-secret root Product Compose setting. Copy to .env to customize it; .env is ignored.
INTAKE_GATE_PORT=8080

# Configure provider credentials through the setup wizard or an explicit environment-variable
# reference. Never place real credentials in this file or commit a populated .env.
